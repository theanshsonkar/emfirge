import { A, DocHeader, Lead, H2, P, Strong, Code, Callout, PrevNext } from "@/components/docs/ui";
import { CodeBlock } from "@/components/docs/code-block";
import { ForkFlow } from "@/components/docs/flow-diagram";
import { prevNext } from "@/lib/nav";
export const metadata = { title: "How the fork works · Emfirge Docs", description: "Read-only scan, modeled branch changes, diffs, and advisory verdicts." };
export default function Page() { const { prev, next } = prevNext("/docs/how-it-works"); return <><DocHeader eyebrow="Concepts" title="How the fork works"><Lead>Emfirge builds a graph from a read-only scan, lets you fork that snapshot, models a proposed change, and reports what the analysis says about the resulting branch.</Lead></DocHeader><H2>The graph</H2><P>A scan reads the cloud through a read-only role and builds a connected infrastructure graph. The graph is the saved base for subsequent analysis; it is not a live write target and it becomes stale as the account changes.</P><H2>The workflow</H2><ForkFlow /><CodeBlock numbered tabs={[{ label: "flow", code: `read-only scan → fork graph → apply modeled branch change
→ diff the branch → re-run analysis lenses → advisory verdict` }]} /><P>Use <A href="/docs/tools/create-branch"><Code>emfirge_create_branch</Code></A> to fork a completed analysis. Add an <A href="/docs/tools/apply-change"><Code>emfirge_apply_change</Code></A> operation to the isolated model, then inspect <A href="/docs/tools/branch-diff"><Code>emfirge_branch_diff</Code></A> and <A href="/docs/tools/branch-verdict"><Code>emfirge_branch_verdict</Code></A>. The verdict combines modeled native findings with available scanner lenses and reports coverage explicitly.</P><H2>Advisory verdicts</H2><P>The combined verdict is <Code>block</Code>, <Code>warn</Code>, or <Code>pass</Code>. It includes score and finding deltas, reachability changes, cost delta information, a privilege-escalation signal, scanner status, coverage warnings, and a summary.</P><Callout type="warning" title="Read coverage warnings"><Code>coverage_warnings</Code> is non-empty when lenses are missing or disabled. In that case the result is degraded: do not describe it as complete coverage or a guarantee of safety. A pass means no modeled concern was found by the available analysis, not that deployment is risk-free.</Callout><H2>Honest limits</H2><P>Branches are models, not deployments. Emfirge does not apply branch changes to AWS, and a security delta does not prove application connectivity or every operational consequence. Re-scan before relying on a result when the live environment has changed.</P><PrevNext prev={prev} next={next} /></>; }

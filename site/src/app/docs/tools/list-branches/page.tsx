import { A, DocHeader, Lead, H2, P, Code, Badge, ParamList, Param, PrevNext, ToolSignature } from "@/components/docs/ui";
import { CodeBlock } from "@/components/docs/code-block";
import { EyeOff } from "lucide-react";
import { prevNext } from "@/lib/nav";
export const metadata = { title: "emfirge_list_branches · Emfirge Docs", description: "List isolated infrastructure branches, optionally filtered by base analysis." };
export default function Page() { const { prev, next } = prevNext("/docs/tools/list-branches"); return <><DocHeader eyebrow="MCP Tools · Branch" title="emfirge_list_branches" mono><Lead>List isolated infrastructure branches, optionally filtered by the analysis ID they are based on.</Lead><div className="mt-5 flex flex-wrap gap-2"><Badge><EyeOff className="size-3.5" /> modeled only</Badge></div></DocHeader><ToolSignature name="emfirge_list_branches" args="base_analysis_id?" returns="[{ branch_id, name, status, base_analysis_id, num_changes }]" /><H2>Parameters</H2><ParamList><Param name="base_analysis_id" type="string">Optionally filter branches by the analysis ID they are based on. Omit it to list all branches.</Param></ParamList><H2>Example</H2><CodeBlock tabs={[{ label: "arguments", code: `{"base_analysis_id":"analysis_123"}` }]} /><H2>Returns</H2><CodeBlock tabs={[{ label: "response", code: `[{"branch_id":"branch_456","name":"restrict-public-access","status":"open","base_analysis_id":"analysis_123","num_changes":1}]` }]} /><P>Each item is compact branch metadata: identity, name, lifecycle status, base analysis, and number of ordered modeled changes. Listing is read-only and does not affect AWS.</P><PrevNext prev={prev} next={next} /></>; }

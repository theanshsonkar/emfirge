import { A, DocHeader, Lead, H2, P, Code, Badge, ParamList, Param, Callout, PrevNext, ToolSignature } from "@/components/docs/ui";
import { CodeBlock } from "@/components/docs/code-block";
import { EyeOff } from "lucide-react";
import { prevNext } from "@/lib/nav";
export const metadata = { title: "emfirge_discard_branch · Emfirge Docs", description: "Discard an isolated branch when it is no longer needed." };
export default function Page() { const { prev, next } = prevNext("/docs/tools/discard-branch"); return <><DocHeader eyebrow="MCP Tools · Branch" title="emfirge_discard_branch" mono><Lead>Discard an isolated branch when it is no longer needed. This does not change AWS.</Lead><div className="mt-5 flex flex-wrap gap-2"><Badge><EyeOff className="size-3.5" /> modeled only</Badge></div></DocHeader><ToolSignature name="emfirge_discard_branch" args="branch_id" returns="{ branch_id, status }" /><H2>Parameters</H2><ParamList><Param name="branch_id" type="string" required>Branch ID returned by <A href="/docs/tools/create-branch"><Code>emfirge_create_branch</Code></A>.</Param></ParamList><H2>Example</H2><CodeBlock tabs={[{ label: "arguments", code: `{"branch_id":"branch_456"}` }]} /><H2>Returns</H2><CodeBlock tabs={[{ label: "response", code: `{"branch_id":"branch_456","status":"discarded"}` }]} /><P>Discard is a soft state change for the isolated branch. A discarded branch remains identifiable but is no longer an open branch for applying changes.</P><Callout type="note">Discarding a branch does not deploy, delete, or roll back any AWS resource.</Callout><PrevNext prev={prev} next={next} /></>; }
